Add sound asset files here (recommended formats: .ogg or .mp3).

Expected filenames (used by the AudioManager):
- startup.ogg      — subtle system startup chime
- unlock.ogg       — success chime for correct PIN
- denied.ogg       — gentle denial / error tone for failed PIN
- save.ogg         — short confirmation sound for saving settings
- panel-open.ogg   — sound when opening settings panel
- panel-close.ogg  — sound when closing settings panel

Place optimized short sounds (~300-800ms). Keep volumes normalized. Use loop=false for all.
